# CMake / Ninja build outputs (also covered by root .gitignore .cxx/ and build/)
/.cxx/
/build/

# cloudflared payload embedded into LS_KTool at build time (downloaded)
/jni/assets/cloudflared-linux-arm64

Pointer*.bin
Pointer_Export.txt
Pointer_Merged.tmp
